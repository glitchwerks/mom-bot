name: Claude CI Fix

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]

permissions:
  contents: write
  pull-requests: write

jobs:
  fix:
    uses: glitchwerks/github-actions/.github/workflows/ci-failure.yaml@v2.6.1
    secrets: inherit
    with:
      auto_apply: true
