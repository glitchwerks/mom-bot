name: Claude PR Review

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  packages: read
  pull-requests: write
  statuses: write

jobs:
  review:
    # SPIKE-261: pinned to spike SHA — revert to @v2 after measurement captured.
    uses: glitchwerks/github-actions/.github/workflows/claude-pr-review.yml@8fd48f927dc01ca73e8f84eba5c6440408e970a9
    with:
      authorized_users: cbeaulieu-gt
      spike_diagnostics: 'true'
    secrets: inherit
